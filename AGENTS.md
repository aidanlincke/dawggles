# Agent Notes

- For any `xcodebuild` command, always set `-derivedDataPath /tmp/<project>-deriveddata` (or another `/tmp` subdirectory).
- Never write `DerivedData` or `.derivedData` inside this repository.
- For user-facing OLED copy, use clear, friendly Apple-style sentence case language. Avoid terse/internal shorthand and always spell out button actions plainly.
